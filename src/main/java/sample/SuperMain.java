package sample;

//This needs to exist as main class cannot extend anything (marked this as main class in pom.xml so it *should* run)
public class SuperMain {
    public static void main(String[] args){
        Main.main(args);
    }
}
